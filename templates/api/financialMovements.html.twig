{% extends 'base.html.twig' %}

{% block title %}financialMovements{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>financialMovements ✅</h1>


        <style>
            th, td {
                padding: 15px;
                text-align: left;
            }
        </style>
        <table border="1">
            <tr><td>id</td>
                <td>bookingDate</td>
                <td>valueDate</td>
                <td>walletId</td>
                <td>amount</td>
                <td>description</td>
            </tr>
            {% for key in financialMovements %}
            <tr><td> {{ key.id }}</td>
                <td>  {{ key.bookingDate }}</td>
                <td>  {{ key.valueDate }}</td>
                <td>  {{ key.walletId }}</td>
                <td>
                    {% for key2 in key.amount %}
                        <br>{{ key2 }}
                    {% endfor %}
                </td>
                <td>
                  {{ key.description}}
                </td>


                {% endfor %}
        </table>

    </div>
{% endblock %}
